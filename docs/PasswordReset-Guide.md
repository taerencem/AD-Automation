# Password Reset + Unlock Script Guide

This script resets a user's password, unlocks the account, and forces a password change at next logon.

## Usage

Run the script:

```powershell
.\Reset-PasswordUnlock.ps1 -Username jdoe -NewPassword "TempPass123!"
